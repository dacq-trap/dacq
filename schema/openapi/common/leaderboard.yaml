type: object
properties:
  rank:
    type: integer
    description: 順位
  team:
    $ref: ./team.yaml
    description: チーム
  highestScore:
    type: number
    description: 最高スコア
  totalSubmissions:
    type: integer
    description: のべ提出回数
  lastSubmittedAt:
    type: string
    format: date-time
    description: 提出日時
required:
  - rank
  - team
  - score
  - totalSubmissions
  - lastSubmittedAt
