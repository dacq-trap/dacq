get:
  summary: pingを飛ばす
  description: pingを飛ばします。
  operationId: getPing
  tags:
    - General
  responses:
    "200":
      description: OK
      content:
        text/plain:
          schema:
            type: string
          example: pong
